# MateClub - Configuration

# REQUIRED - Server port (default: 3000)
PORT=3001

# OPTIONAL - Database path (default: /app/data/mateclub.db)
# DATABASE_PATH=/app/data/mateclub.db

# Note: No ADMIN credentials needed - first admin is created via /setup on first launch
